# manjaro gnome

# remove firefox and other unneeded software

# rate mirrors
  $ sudo pacman-mirrors --fasttrack

# update

# install chrome

# Mac theme
# download from
#     https://github.com/paullinuxthemer/Mc-OS-themes
# to ~/.themes

# icons theme
  la-capitaine

# install (dash to panel) extension

# terminal theme
  https://github.com/Mayccoll/Gogh

# development tools
  install neovim

  install python-pip python2-pip
  $ mkdir ~/.python_venv

# clone config repo
  config files for git, bash, nvim (copy to .config/nvim/) etc

# deep learning
  $ sudo apt install libopenblas-dev liblapack-dev
  $ sudo apt install python-numpy python-scipy python-matplotlib python-yaml
  $ sudo apt install libhdf5-serial-dev python-h5py
  $ sudo apt install graphviz
  $ pip3 install pydot-ng
  $ pip3 install tensorflow
  $ pip3 install keras
  $ sudo snap install pycharm-community --classic



